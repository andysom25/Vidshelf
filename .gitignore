# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Downloads / media output
*.mp4
*.mkv
*.webm
*.m4a
*.mp3
*.part
*.ytdl

# Env / secrets
.env
*.log
cookies.txt

# Persistent state. Since v1.1.0 these live together in ./data (see state.py);
# the bare filenames stay listed so an upgraded checkout doesn't accidentally
# commit leftovers from the pre-v1.1.0 layout.
data/
config.json
active_downloads.json
downloaded_videos.json

# Aider
.aider*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code — settings.local.json is per-developer, not shared config
.claude/settings.local.json

# Per-deployment compose overrides (e.g. running the published GHCR image
# instead of building from source). Which image a machine runs is a local
# choice; the tracked docker-compose.yml stays the build-from-source path.
docker-compose.override.yml
docker-compose.override.yaml
